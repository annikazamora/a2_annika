function drawEnd() {
  // Background colour for the home screen
  background(180, 225, 220); // soft teal background

  // ---- Title text ----
  fill(30, 50, 60);
  textSize(20);
  textAlign(CENTER, CENTER);
  text("womp womp game is over", width / 2, 280);
}
